namespace ReillyDigital.Observables;

/// <summary>
/// An enumeration to configure how a <see cref="ObservableWebSocket" /> will be observed.
/// </summary>
public enum ObservableWebSocketMode
{
	ObserveFullMessages,
	ObservePartialMessages
}
